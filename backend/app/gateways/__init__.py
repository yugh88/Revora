"""Payment gateway implementations. BUILD_SPEC Section 5.

Both gateways satisfy the same PaymentGateway interface, which is what makes the
runtime "Built-in Simulator | Razorpay Test Sandbox" toggle possible. The
built-in simulator is the default and has zero external dependencies.
"""
